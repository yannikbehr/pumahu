image: tmaier/docker-compose:latest

services:
  - docker:dind

before_script:
  - docker info
  - docker-compose --version

stages:
  - build

build:
  tags:
    - linux
  stage: build
  script: |
    . ./buildnrun.sh -b
